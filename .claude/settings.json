{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "cd \"/c/Code/Games/worldhopper\" && git add -A && git diff --cached --quiet || git commit -m \"Auto-save: $(git diff --cached --name-only | tr '\\n' ' ' | sed 's/ $//')\"",
            "statusMessage": "Committing changes..."
          }
        ]
      }
    ]
  }
}
